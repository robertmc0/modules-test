{
  "$schema": "https://schema.management.azure.com/schemas/2019-08-01/managementGroupDeploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.9.1.41621",
      "templateHash": "6167656313912530324"
    }
  },
  "parameters": {
    "name": {
      "type": "string",
      "metadata": {
        "description": "The resource name."
      }
    },
    "description": {
      "type": "string",
      "metadata": {
        "description": "The policy set definition description."
      }
    },
    "displayName": {
      "type": "string",
      "metadata": {
        "description": "The display name of the policy set definition."
      }
    },
    "parameters": {
      "type": "object",
      "defaultValue": {},
      "metadata": {
        "description": "Optional. The policy set definition parameters that can be used in policy definition references."
      }
    },
    "policyDefinitions": {
      "type": "array",
      "metadata": {
        "groupNames": "The name of the groups that this policy definition reference belongs to.",
        "parameters": "The parameter values for the referenced policy rule. The keys are the parameter names.",
        "policyDefinitionId": "The ID of the policy definition or policy set definition.",
        "policyDefinitionReferenceId": "A unique id (within the policy set definition) for this policy definition reference.",
        "description": "Policy definition references."
      }
    }
  },
  "resources": [
    {
      "type": "Microsoft.Authorization/policySetDefinitions",
      "apiVersion": "2021-06-01",
      "name": "[parameters('name')]",
      "properties": {
        "copy": [
          {
            "name": "policyDefinitions",
            "count": "[length(parameters('policyDefinitions'))]",
            "input": {
              "policyDefinitionId": "[parameters('policyDefinitions')[copyIndex('policyDefinitions')].policyDefinitionId]",
              "parameters": "[parameters('policyDefinitions')[copyIndex('policyDefinitions')].parameters]"
            }
          }
        ],
        "description": "[parameters('description')]",
        "displayName": "[parameters('displayName')]",
        "parameters": "[parameters('parameters')]",
        "policyType": "Custom"
      }
    }
  ],
  "outputs": {
    "name": {
      "type": "string",
      "value": "[parameters('name')]",
      "metadata": {
        "description": "The name of the deployed policy set definition."
      }
    },
    "resourceId": {
      "type": "string",
      "value": "[extensionResourceId(managementGroup().id, 'Microsoft.Authorization/policySetDefinitions', parameters('name'))]",
      "metadata": {
        "description": "The resource ID of the deployed policy set definition."
      }
    }
  }
}